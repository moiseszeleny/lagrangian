% Auto-generated by research/thdm_s3/derive.py -- do not edit by hand.
% Printable derivation appendix: check the algebra line by line on paper.
\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage[margin=2.2cm]{geometry}
\setlength{\parindent}{0pt}
\title{Derivation appendix: 03 --- scalar decays and LFV rates\\[4pt]\large\normalfont VV couplings, loop-induced modes and LFV rates}
\date{}
\begin{document}
\maketitle

\subsection*{1.\quad $h_0$ is gauge-phobic exactly, and the $VV$ couplings obey a sum rule}
\emph{derived here} --- \S2\par\medskip
\textbf{Claim}\par\nopagebreak
\begin{equation*}\small g_{h_iVV} = g^{\rm SM}\,(R_S^{\mathsf T}\hat n)_i = g^{\rm SM}(\cos\delta,\,0,\,\sin\delta),\qquad \sum_i g_{h_iVV}^2 = (g^{\rm SM})^2 \end{equation*}
\textbf{Result}\par\nopagebreak
\begin{equation*}\small {R_S^{\mathsf T}\hat n} = \left[\begin{matrix}\cos{\left(\delta \right)} & 0 & \sin{\left(\delta \right)}\end{matrix}\right] \end{equation*}
\textbf{Independent checks}\begin{itemize}\itemsep0pt
\item $R_A$'s first column verified to be the unit vacuum direction
\item the SM reference read off the $\delta\to0$ limit, not typed in
\item $W$ and $Z$ couplings both equal $g^{\rm SM}\times$ the same projection
\item the zero survives a generic vacuum angle $\varphi_v$, so it is orthogonality and not the $\sqrt3$ alignment
\end{itemize}
\textbf{Would be falsified by:} a non-zero middle projection at generic $\varphi_v$, or a $\delta\to0$ reference differing from $i g_w m_W$\par
\medskip\hrule\medskip
\subsection*{2.\quad $\mathrm{lepton\_mus}(\mu_3)$ inverts the notebook-02 \S{}5 relations}
\emph{derived here} --- \S7\par\medskip
\textbf{Claim}\par\nopagebreak
\begin{equation*}\small \mathrm{svd}\big(M_\ell(\mu_1,\ldots,\mu_4)\big) = (m_e,\,m_\mu,\,m_\tau)\qquad\text{for every }\mu_3\in(m_\mu,\,m_\tau) \end{equation*}
\textbf{Independent checks}\begin{itemize}\itemsep0pt
\item singular values match the PDG masses to $10^{-9}$ relative across the whole $\mu_3$ scan
\item the Yukawas genuinely vary, so the check is not trivial
\item a 1\% perturbation of $\mu_4$ breaks it
\end{itemize}
\textbf{Would be falsified by:} the spectrum matching at one $\mu_3$ but not across the scan, which would mean the inversion is only locally right\par
\medskip\hrule\medskip
\subsection*{3.\quad The one-loop form factors $A_{1/2}$, $A_1$, $A_0$ and the LFV width formula}
\emph{imported} --- \S5--6 (from [Djouadi08])\par\medskip
\textbf{Claim}\par\nopagebreak
\begin{equation*}\small \Gamma(h\to\ell_a\bar\ell_b+\bar\ell_a\ell_b) = \frac{m_h}{8\pi}\big(|Q_{ab}|^2+|Q_{ba}|^2\big) \end{equation*}
\textbf{Independent checks}\begin{itemize}\itemsep0pt
\item $A_{1/2}\to4/3$ and $A_1\to-7$ in the decoupling limit
\item $A_0\to-1/3$, the standard charged-scalar value
\item the SM diphoton width is reproduced against PDG
\end{itemize}
\textbf{Would be falsified by:} a decoupling limit disagreeing with the literature values\par
\medskip\hrule\medskip
\end{document}