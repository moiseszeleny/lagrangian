__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
*.so
build/
dist/
docs/_build/
docs/api/generated/

# generated UFO models from scripts
scripts/FEYNLAG_SM_UFO/
scripts/*_UFO/

# heavy scan intermediates from research/ projects; small results/*.json
# artifacts ARE committed (see research/README.md)
research/**/cache/

# LaTeX build products of the generated derivation appendices (the .tex the
# notebooks emit IS committed; the PDF and friends are rebuilt on demand)
research/**/*.pdf
research/**/*.aux
research/**/*.out
research/**/*.synctex.gz
research/**/*.fdb_latexmk
research/**/*.fls
research/**/*.log

# graphify knowledge-graph output (graph.json, HTML viz, obsidian vault,
# wiki, cypher/svg/graphml exports — all nested under graphify-out/) and
# the raw/ corpus `graphify add <url>` fetches external content into
graphify-out/
raw/
